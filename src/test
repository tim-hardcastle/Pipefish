def

foo (x int, y string) : "1"
foo (x int, y float64) : "2"
foo (x single, y bool) : "3"

foo (S tuple) | (T tuple) : S

bar (S tuple) | (T tuple) : T


foo (L list) | (K list) : "B"

//bar(x int, y bool, z struct) : "4"

//bar(j int, i tuple) zort: "5"

troz(i int, b bool): "troz"

spoit(x tuple): x

ta (x int) tb tb2 (y int) tc:
    x + y


thang : 1

a = spoit 1

b = ta 6 tb tb2 7 tc

cmd

reterr(x int) :
    return error "Ooops!"

greet :
    return "Hello world!"
