var

x = 42
c code? = NULL

def

capture (exp ast) : exp

total (exp ast) of (ix ast) over (p pair):
    (while condition do action to p[0], 0)[1]
given:
    condition(j, s): j < p[1]
    action(ix ident, s) : (eval ix) + 1, s + (eval exp)






