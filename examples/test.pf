def

foo(r rune, bounds ... pair) :
    bounds

isIn(r rune, bounds ... pair) : 
	from flag = false for _::bound = range bounds :
		bound[0] <= codepoint(r) and codepoint(r) < bound[1] :
			true
		else :
			continue