cmd

foo(x) :
    return 42, 42

def

f = func(x) : 2 * x

g = func(x) : 2 * x, 3 * x

h = func(x, y) : x + y


zort(x int, y string) :
    troz(x, y)
given
    troz(x, y) : string(x) + "  " + y

(x) garp : x




