newtype

Rotatable = interface :
    rotate(x self) -> self

def 

sum(L list) :
    from a = L[0] for _::v = range L[1::len L] :
        a + v

rotAll(L list) :
    from a = [] for _::v = range L :
        a + [rotate v]


