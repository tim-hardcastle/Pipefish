def

zort(i, j) :
    i + glerp i 
given :
    glerp(x) :
        j * x

newtype

Color = enum RED, WHITE, BLUE

Foo = struct(a int, b string)

def

nerp(s type?) :
    s in null :
        "Value is null"
    else:
        s[0]

var

myFoo = 42