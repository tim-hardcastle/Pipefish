external

qux::"examples/qux.pf"

def

blurp (x varchar(10)) :
    x + " is a tiny string"

blurp (x varchar(20)) :
    x + " is a longer string"

blurp (x string) :
    x + " is a much longer string"

private

fuzz(x string) :
    "**" + x + "**"

var 

x = 42

private 

y = 43

def

troz(x string) :
    fuzz x

const private

PI = 3.141592


