import 

"zort.pf"

newtype // Some modular arithmetic.

Z = clone{i int} int :
    0 <= that
    that < i

Vec = clone{i int} list :
    len that == i

Foo = struct{i int}(name string, age int) :
    that[age] < i

make Z{5}, Z{12}, Vec{3}, Foo{18}



