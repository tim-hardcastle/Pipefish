var

x = 3

newtype

Z = clone{i int} int :
    0 <= that
    that < i

Foo = struct{i int}(name string, age int) :
    that[age] < i

make Z{5}, Foo{18}

def

(x Z{5}) + (y Z{5}) :
    Z{5}(int(x) + int(y) mod 5)