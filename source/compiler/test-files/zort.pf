// Exists to be imported by the import_test.pf and external_test.pf files.

cmd 

foo :
    post "foo"

bar() :
    post "bar"

qux(i) :
    post "qux"

def

square(x int) :
    x * x

newtype

Color = enum RED, ORANGE, YELLOW, GREEN, BLUE, PURPLE

Shade = enum LIGHT, MEDIUM, DARK

Tone = struct(shade Shade, color Color)

MyType = abstract Shade/Color/Tone

Person = struct(name string, age int)

Qux = clone int
