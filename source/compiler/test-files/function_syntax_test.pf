def

foo(x string) :
    "foo " + x

(x string) zort : 
    x + " zort"

(x string) troz (y string) :
    x + " troz " + y

moo (x string) goo :
    "moo " + x + " goo"

flerp (x string) blerp (y string) :
    "flerp " + x + " blerp " + y

qux :
    "qux"

cmd

merp(x, y) :
    try :
        post (blerp x)
    else :
        post "foo"
given :
    blerp(x) :
        x
