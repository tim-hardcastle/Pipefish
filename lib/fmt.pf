import

gocode "fmt"

def

errorf(format string, a ... any?) : gocode {
    return fmt.Errorf(format, a...)
}

sprint(a ... any?) : gocode {
    return fmt.Sprint(a...)
}

sprintf(format string, a ... any?) : gocode {
    return fmt.Sprintf(format, a...)
}

sprintln(a ... any?) : gocode {
    return fmt.Sprintln(a...)
}
